# Implementation Plans

Generated by `/improve` on 2026-06-13 at commit `b1274d9`. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-epub-filename-sanitize.md) | Sanitize EPUB upload filename | P1 | S | — | DONE |
| [002](002-project-status-running-paused.md) | Fix project status running/paused | P1 | S | — | DONE |
| [003](003-db-voice-methods-and-assembled-status.md) | Voice DB methods + batch progress + assembled fix | P2 | S | — | DONE |
| [004](004-casting-director-api.md) | Wire CastingDirector to API | P2 | M | — | DONE |
| [005](005-ui-tests-and-m4b-api-test.md) | UI component tests + M4B API test | P2 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED (with rationale)

## Dependency notes

All five plans are independent — none blocks another. Plans 001–003 are S effort;
execute them before the M-effort plans (004, 005) to gain confidence on the easy wins first.

Note: Plan 003 Step 5 (`list_projects` batch query) calls `_project_status` with
arguments. If Plan 002 has already landed, `_project_status` takes 3 args; if not,
it takes 1. The executor for Plan 003 must check which signature is live and apply
the call accordingly (plan 003 Step 5 documents this).

## Findings considered and rejected

- **Profile update before existence check** (`app.py:187`): `UPDATE` silently no-ops on
  missing ID; 404 is still returned correctly; no data risk; swap is purely cosmetic.
- **Voice assignment before character existence check** (`app.py:347`): same pattern, same verdict.
- **PipelineManager thread safety**: GIL protects simple attribute reads/writes; FastAPI's
  async event loop is single-threaded, making the check-then-act window vanishingly small.
- **`chapter_range` IndexError risk**: Pydantic `field_validator` at `models.py:32-43`
  rejects out-of-spec inputs before `app.py:216` is reached.
- **Python `pip` CVEs**: Affects the pip tool, not runtime code. Upgrade pip but not a code finding.
- **PostCSS XSS (npm)**: Build-tool dependency, not served to users. Low priority.
- **N+1 on `list_projects`** (originally a separate finding): Bundled into Plan 003 Step 5.
